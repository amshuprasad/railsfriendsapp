<div class="card" style="width: 18rem;">
  <div class="card-body">
    <%= render @friend %>
    <%= link_to "Edit", edit_friend_path(@friend), class: "btn btn-secondary" %> <%= link_to "Back", friends_path, class: "btn btn-secondary" %> 
    <br/><br/>
  </div>
</div>
